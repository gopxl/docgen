name: 'gopxl docs'
author: 'Mark Kremer'
description: 'A static website generator for the gopxl Markdown documentation'
inputs:
  docs-directory:
    description: 'Path to the documentation directory inside the repository'
    required: false
    default: 'docs/'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - "-url"
    - "$GITHUB_REPOSITORY_OWNER.github.io/gopxl-docs-generator"
    - "-repository"
    - "./"
    - "-docs"
    - ${{ inputs.docs-directory }}
    - "-dest"
    - "generated/"
